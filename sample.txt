<div class="text-center" style="max-width: 10%;background-color: grey;margin-left:45%;border-radius: 2%;color: white;padding-left: 2%;">
  <div class="col-md-4 text-center" *ngIf="start" style="color: white;">
  <div class="col-md-12" class="ques">
    <h5>{{currentQuestionSet.qid}} {{currentQuestionSet.question}}</h5>
     <div *ngFor="let o of options">
      <div *ngIf="currentQuestionSet.qid === o.qid">
       <input type="radio" name="{{currentQuestionSet.qid}}" value="{{o.optionid}}" [(ngModel)]="currentQuestionSet.selected" />{{o.name}}
     </div>
    </div>
   </div>
  </div>
 
 <div class="col-md-4" *ngIf="start">
     <h5>{{currentIndex+1}}/{{questions.length}}</h5>
     <input *ngIf="(currentIndex+1) != questions.length" type="button" class="btn" value="Next" (click)="next()"  class="btn btn-success"/>
 
   </div>
 
  <div class="col-md-4" >
     <input *ngIf="!start" type="button" class="btn" value="{{buttonname}}" (click)="startQuiz()" />
   </div>
   <div class="col-md-4" *ngIf="start">
     <input type="button" class="btn" value="Submit" (click)="submit()" class="btn btn-warning"/>
   </div>
 
   <div *ngIf="gameover"><h5>Result:  {{correct}}/{{questions.length}}</h5></div>
 </div>
